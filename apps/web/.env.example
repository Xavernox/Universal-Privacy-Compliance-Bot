# MongoDB Configuration
MONGODB_URI=mongodb://localhost:27017/upcb-mvp

# JWT Configuration
JWT_SECRET=your-super-secret-jwt-key-change-in-production
JWT_EXPIRES_IN=7d

# Admin API Configuration (for /api/admin/* endpoints)
ADMIN_USERNAME=admin
ADMIN_PASSWORD=secure-admin-password

# Application Configuration
NODE_ENV=development
NEXT_PUBLIC_APP_NAME=U-PCB MVP
NEXT_PUBLIC_APP_VERSION=1.0.0

# Optional: Email Service Configuration (for password reset, notifications)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your-email@gmail.com
# SMTP_PASS=your-app-password

# Optional: Social Login (if needed later)
# GOOGLE_CLIENT_ID=your-google-client-id
# GOOGLE_CLIENT_SECRET=your-google-client-secret